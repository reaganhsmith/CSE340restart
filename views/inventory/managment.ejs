<!-- THE CONTENT HEADING -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<a href="./add-classification"> Create New Classification </a>
<a href="./add-inventory"> Create New Inventory</a>
