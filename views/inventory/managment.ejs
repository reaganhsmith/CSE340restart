<!-- THE CONTENT HEADING -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>


    <!-- Messages or error mesages  -->
<%- messages() %>


<% if (errors) { %>
  <ul class="notice">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>

<div class="manage">
<a href="./inv/add-classification"> Create New Classification </a>
<a href="./inv/add-inventory"> Create New Inventory</a>
</div>

<div class="manageInv">
<h2>Manage Inventory</h2>
<p>Select a classification from list to see what cars belong to it.</p>
<%- classificationSelect %>
<table id="inventoryDisplay"></table>
</div>

<noscript> Javascript must be enabled to use this page </noscript>

<script src="../../js/inventory.js"></script>